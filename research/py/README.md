# NexusQuant Research Python Skeleton

本目录是 GateF-2 启动的最小 Python 研究骨架。

当前只包含：

- `strategies/`：最小策略接口样例
- `datasets/`：数据目录说明
- `fixtures/`：样本历史数据

当前明确不包含：

- Java / Python runtime bridge
- notebook 体系
- 指标评估
- 实盘执行接入
