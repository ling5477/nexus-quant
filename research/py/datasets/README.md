# Datasets

本目录用于放置研究阶段使用的数据说明或数据入口占位。
Python 的 dataset manifest 实现在 `../src/nq_research/dataset/`；本目录不参与 package 安装。

GateF-2 当前只提供 `../fixtures/btcusdt_1m_sample.csv` 作为最小样本，
后续 GateF-3 / GateF-4 再决定是否引入更系统化的数据组织方式。
