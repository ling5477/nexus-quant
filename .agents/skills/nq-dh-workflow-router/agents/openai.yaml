interface:
  display_name: "NQ Workflow Router"
  short_description: "Classify NQ tasks and select the minimum safe Skill set"
  default_prompt: "Use $nq-dh-workflow-router to resolve NexusQuant authority, classify task and risk, choose one primary Skill, and define a minimal safe scope."
