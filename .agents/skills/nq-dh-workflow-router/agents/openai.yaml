interface:
  display_name: "NQ-DH Workflow Router"
  short_description: "Classify NQ/DH tasks and route tools safely"
  default_prompt: "Use $nq-dh-workflow-router to classify a NexusQuant or Decision Hub task before choosing tools."
