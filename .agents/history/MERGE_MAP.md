# HISTORICAL / NON_AUTHORITATIVE

原 `MERGE_MAP.md` 记录了早期 Skill 合并来源。该材料已由 `.agents/README.md` 的 active 清单和 `scripts/docs/agent-workflow-policy.json` 取代；不得用于 current routing 或权限判定。

原始全文保存在本次 GateAUDIT 的 immutable baseline snapshot 与隔离审计对象中。
