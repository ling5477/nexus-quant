# Post-GateAUDIT shared capability matrix

Candidate: `0ee643c813f890a8215876eabe608dcebcea6081` on `refactor/post-gateaudit-repository-consolidation` at discovery. This matrix records the post-topology production scan. Paths below are relative to the repository root. `owner → consumer` denotes dependency direction. No cross-language implementation or new Maven module is introduced.

Discovery used production-only searches for SHA-256, JSON, time, number/string normalization, status/error mapping, redaction, HTTP/retry, and utility-like APIs, followed by source-body comparison and call-site inspection. `rg` searches covered `backend/**/src/main/**/*.java`, `frontend/src/**/*.{ts,tsx}`, `research/py/src/nq_research/**/*.py`, and active scripts; test and frozen evidence were classified separately. Exact source text was only a candidate signal. The contract decision includes all consumers of each listed implementation, its inputs, canonicalization, output, exception, side effect, security and runtime context. Verification names indicate the actual regression gate, not a claim that every failure is resolved.

The following table is the complete disposition of the 26 candidate groups found in this scan. Each row supplies `candidateGroupId`, `language`, `capability`, `members`, `currentOwners`, `consumerGraph`, `inputContract`, `normalizationRules`, `outputContract`, `errorContract`, `sideEffects`, `securityBoundary`, `runtimeContext`, `dependencyDirection`, `similarityEvidence`, `semanticDecision`, `canonicalOwner`, `action`, and `verification`. `none` means no side effect beyond allocation or formatting. Repeated call sites within one domain are represented as one group when the same contract decision applies.

| ID / language / capability | Members; owners → consumers | Input / normalization → output; error | Side effects; security; runtime; dependency | Similarity evidence → semantic decision; canonical owner; action; verification |
| --- | --- | --- | --- | --- |
| J01 Java nullable text selection | `nq-contracts` Place/Cancel commands, `nq-core` Place/Cancel requests and three strategy overview services, `nq-adapter-api` Order/Cancel requests, `nq-research` Research/Backtest config → their constructors/read models | Nullable strings; `isBlank`, `trim`, caller-supplied fallback order → trimmed first value or null; no new exception | none; caller retains trading/adapter/research validation and authorization; request construction/read-only projection; all consumers already depend on `nq-common` | Six identical two-value bodies, three identical multi-value bodies, two single-value bodies → **CONSOLIDATED_TO_CANONICAL_OWNER**; `nq-common.text.NullableText`; migrated 11 direct consumer files and removed 11 bodies; compatibility vectors, affected reactor, architecture tests |
| J02 Java canonical business digest | `nq-core` `CanonicalDigestSupport`, `RiskLimitSetCanonicalEncoder`, `ExecutionIntentCanonicalEncoder` → risk and execution identity consumers | Domain object/intent; different field order, scale, quoting and UTC formatting → lowercase digest; domain validation exceptions differ | none; LIVE/execution identity; runtime transaction path; keep `nq-core` local | Common SHA-256 calls but canonical bytes and scale differ → **CANONICALIZATION_CONTRACT_DIFFERENT**; existing domain owners; retain; source contract and no-change diff |
| J03 Java strategy artifact / release identity | `nq-core` `TrustedRootStrategyArtifactVerifier`, `VerifiedOpenStrategyArtifactReader`, `StrategyReleaseManifestFingerprinter`, `AdmissionGuardFingerprinter`, `ReleaseToShadowAdmissionService` → release/admission | File bytes, manifest fields or admission facts; streaming/ordering/length/separators differ → digest or verified identity; size/path/validation failures differ | file reads for verifiers; release trust boundary; offline admission; domain owner only | SHA-256 and hex overlap, hash preimages and I/O contract differ → **CANONICALIZATION_CONTRACT_DIFFERENT**; existing artifact/release owners; retain; source and test scan |
| J04 Java credential versus diagnostic digest | `ScopedCredentialReference`, `BinanceWsClient`, `PrivateReadonlyDiagnosticEndpointContract`, fake venue launcher → credential reference / logs / diagnostics | Credential reference or diagnostic text; truncation, salt/prefix, or fallback differ → stable reference or safe display hash; error behavior differs | none or logging; credential versus diagnostic boundary; runtime/fake fixture; keep local | Same SHA-256 algorithm, different disclosure and failure semantics → **SECURITY_BOUNDARY_DIFFERENT**; respective owners; retain; source review |
| J05 Java venue rule versus request digest | `VenueRuleChecksumCalculator`, `ValidationReviewRequestHasher`, `ShadowRunCreationPlan` → venue catalog / review / idempotency | Venue facts versus sorted request JSON versus header; distinct nulls/order/normalization → different checksum/key; validation exceptions differ | none; identity is domain scoped; command path; keep `nq-core` domains | Digest hex only; preimage contracts differ → **CANONICALIZATION_CONTRACT_DIFFERENT**; respective domain owners; retain; source review |
| J06 Java advisory lock digest | `PostgresAdvisoryLockKeyMapper` and digest utilities → scheduler locking | Domain-separated bytes and big-endian prefix → integer advisory key, unlike hex identities; fail-closed SHA error | DB lock acquisition downstream; scheduler isolation; infrastructure runtime; `nq-infra` local | SHA-256 primitive only → **CANONICALIZATION_CONTRACT_DIFFERENT**; lock mapper; retain; source review |
| J07 Java provider signing | OKX signer, Binance HMAC/Ed25519, DH dry-run signer → provider adapters | Provider-specific timestamp, HTTP payload and key material → signature/base64/hex; provider-specific failures | network request signing; credential boundary; provider runtime; no cross-provider owner | Cryptographic primitives overlap; protocol bytes and key types differ → **PROTOCOL_SPECIFIC**; provider owners; retain; source review |
| J08 Java JSON serialization | Spring JSON, adapter raw payloads, JSONB mappings, historical artifact readers, canonical hash writers → HTTP/DB/artifact consumers | Object/domain-specific mapper settings and ordering → JSON or canonical bytes; checked/unchecked errors differ | possible DB/network at caller; canonical identity boundary only for hash writers; mixed runtime; no generic mapper edge | `new ObjectMapper`/`readTree` repetition is use of a library, not one contract → **CANONICALIZATION_CONTRACT_DIFFERENT**; existing owners; retain; source review |
| J09 Java clocks and timestamps | business `Clock`, Binance synchronized time, OKX signing time, fake/test clocks → adapters and services | Wall clock or injected clock plus provider offset/precision → Instant or signature timestamp; timeout/parse failures differ | time source read; signed-wire boundary; provider/business/test contexts; local | `Instant.now` and ISO formatting overlap only → **RUNTIME_CONTEXT_DIFFERENT**; existing owners; retain; source review |
| J10 Java numeric normalization | `nq-common.numeric.NumericPolicy`, venue precision, risk/accounting scales, `CanonicalDigestSupport.money` → their own domains | Decimal plus policy/scale; `UNNECESSARY`, strip-zero, venue rounding vary → decimal/validation; exceptions differ | none; money/risk boundary; trading runtime; domain owners use common only for proven generic policy | `BigDecimal.setScale` overlap, precision and rejected values differ → **INTENTIONAL_DOMAIN_LOCAL**; existing owners; retain; source review |
| J11 Java sensitive rendering | `SensitiveLogSanitizer`, `PublicMarketDataRedactor`, response suppression, Binance WS masking → log/outbound/API | Rendered log line versus endpoint/query/error/payload; regex and allowlist differ → redacted string or suppressed field; null/error contracts differ | logging/output; credential protection at distinct sinks; runtime boundary; observability is final log owner | Similar secret markers, different placement and safe fields → **SECURITY_BOUNDARY_DIFFERENT**; existing sink owners; retain; rendered-log protection unchanged |
| J12 Java error mapping | API exception mapping, adapter error classifiers, venue result categories → HTTP/adapter callers | Domain error/HTTP/provider code → response or retry category; fallback differs; exceptions differ | response emission; API versus provider boundary; runtime; existing modules | Similar status words do not imply same error contract → **FALSE_SIMILARITY**; existing owners; retain; source review |
| J13 Java HTTP/retry support | Public market data outbound settings/client, Binance/OKX HTTP and private-read transports → adapters | Idempotent public GET versus signed private request; timeout/backoff/redirect policy differ → transport result; retries and unknown outcomes differ | network I/O; public/private credential boundary; provider runtime; adapter-specific | Similar client construction, different retry legality → **PROTOCOL_SPECIFIC**; existing transport owners; retain; source review |
| J14 Java trace and generated IDs | `TraceIdContext`, strategy operator/review IDs, fake venue IDs → observability/read models | Trace token versus canonical seeded facts; prefix/truncation/normalization vary → trace or stable ID; validation differs | none; trace versus identity boundary; runtime/fake; existing owners | Similar UUID/hash patterns but different identity lifetimes → **INTENTIONAL_DOMAIN_LOCAL**; existing owners; retain; source review |
| J15 Java test support | Test builders, fake clocks, synthetic fixture hashers, L6 formal harness → tests only | Test-specific fixtures and oracles → local proof data; test assertion errors | fixture writes only; no production authority; test runtime; no production dependency | Repeated setup/hashes are test-only and L6 identities are fixed → **TEST_ONLY_DUPLICATION**; test owners; retain; test/production separation scan |
| J16 Java one-field nullable normalization | Account/credential services, market-data DTOs, incident/strategy read models, API controller, research services → their own validators | Domain field plus surrounding required/length/security checks; some blank→null, others blank→empty, some throw → domain field; errors differ in whole constructor | none; credential, reconciliation, DTO and API boundaries differ; runtime domain models; private local methods | One-line `trim` bodies recur, but composite input/error/security contracts are not interchangeable → **INTENTIONAL_DOMAIN_LOCAL**; field owners; retain; call-site and method scan |
| F01 Frontend fixed precision number format | `components/nq/NqNumericText.tsx` and `nq-design-system/format/nqFormat.ts` → dashboard, charts, design cells | Nullable number/string plus precision/signed; same zh-CN grouping, fixed fraction, nonfinite pass-through → string; no throw | none; presentation only; browser; component → design system | Exact behavior for old option surface; design system adds optional grouping without changing defaults → **CONSOLIDATED_TO_CANONICAL_OWNER**; design system `formatNqNumber`; component re-export and consumers preserved; 40 unit tests/build |
| F02 Frontend Shadow Run status tone | List and detail page local `statusTone` → two pages | Nullable status; same uppercase/substring precedence → tone; no throw | none; diagnostic-only UI; browser; pages → local page-domain function | Identical bodies and status meanings → **CONSOLIDATED_WITHIN_DOMAIN**; `pages/shadow-runs/statusTone.ts`; two consumers migrated; type/build and Shadow Run E2E attempted |
| F03 Frontend date display | `utils/formatters.ts` `formatDateTime`, SystemError current time, chart/event timestamps → pages | Server timestamp versus current browser time; absent value and locale options differ → display text; invalid-date behavior differs | wall clock read for SystemError; UI only; browser; pages → utility where compatible | Shared `Date/toLocaleString` syntax with different time source → **RUNTIME_CONTEXT_DIFFERENT**; existing utility/page owners; retain; source review |
| F04 Frontend other status tones | Design-system `statusToneOf`, runtime readiness `statusTone`, Shadow Run domain tone → pages | Generic status versus explicit safety states versus diagnostic review; different fallback and READY/UNKNOWN meaning → tone; no throw | none; safety presentation; browser; domain → design system only when compatible | Same output union, conflicting status meaning → **INTENTIONAL_DOMAIN_LOCAL**; respective owners; retain; source review |
| F05 Frontend errors, loading and query helpers | `api/errors.ts`, Nq states, form-specific `normalize*`, `utils/formatters.ts` → pages | API error versus empty/loading state versus form input; precedence and side effects differ → text/component/request; validation errors differ | query fetch remains at caller; UI/API boundary; browser; existing owner graph | Similar render/filter shapes, no duplicate full contract → **INTENTIONAL_DOMAIN_LOCAL**; existing owners; retain; source review |
| P01 Python stable offline JSON digest | `dataset/manifest.py`, `experiment/metadata.py`, `evaluation/parameters.py` → dataset/experiment/parameter IDs | JSON mapping; `ensure_ascii=False`, sorted keys, compact separators, UTF-8, SHA-256 → lowercase hex; native JSON `TypeError`/`ValueError` | none; offline research only; Python process; domains → `nq_research.serialization.canonical` | Three exact bodies and identical Unicode/nesting vector → **CONSOLIDATED_TO_CANONICAL_OWNER**; serialization canonical; three consumers migrated and bodies removed; compatibility vectors/pytest/mypy/Ruff |
| P02 Python artifact checksum | `evaluation/artifacts.py` checksum and artifact ID versus P01 → artifact readers | Checksum excludes `checksum` key, validates JSON with custom `TypeError`; ID payload differs → checksum/artifact ID; custom error contract | local artifact I/O at wrapper; offline artifact integrity; Python process; artifact owner local | Same JSON bytes/SHA primitive but omission and validation differ → **CANONICALIZATION_CONTRACT_DIFFERENT**; artifacts; retain; artifact tests |
| P03 Python dataset file checksum | `dataset/manifest.py` streaming CSV checksum versus P01 → dataset manifest | Raw file bytes and record order → content checksum; file I/O failures | local file read; dataset integrity; Python process; dataset owner local | SHA-256 only, not canonical JSON → **CANONICALIZATION_CONTRACT_DIFFERENT**; dataset; retain; dataset tests |
| P04 Python JSON validation probe | `evaluation/parameters.py` `ensure_json_serializable`, artifact canonical writer, CLI summary → evaluation/CLI | Probe accepts arbitrary JSON value and translates errors; canonical identity accepts mapping and preserves native error → none or JSON string; errors differ | none; offline artifact validation; Python process; existing direction | Same `json.dumps` flags, validation/output contract differs → **CANONICALIZATION_CONTRACT_DIFFERENT**; parameter/artifact owners; retain; tests |
| S01 Scripts and CI | Active stage guards and fixture helpers versus frozen evidence scripts/receipts → CI only | Stage manifest/policy versus historical run artifact → guard decision/evidence; failure policy differs | local file reads; evidence authority; CI/historical context; no runtime dependency | Python/PowerShell utility syntax is not shared production capability → **FALSE_SIMILARITY**; active guard owners; retain; stage guards |

## Consolidation accounting and verification

`DUPLICATE_CAPABILITY_GROUPS_DISCOVERED=26`; `CONSOLIDATED_TO_CANONICAL_OWNER=3`; `CONSOLIDATED_WITHIN_DOMAIN=1`; `INTENTIONAL_DOMAIN_LOCAL=5`; `PROTOCOL_SPECIFIC=2`; `CANONICALIZATION_CONTRACT_DIFFERENT=8`; `SECURITY_BOUNDARY_DIFFERENT=2`; `RUNTIME_CONTEXT_DIFFERENT=2`; `TEST_ONLY_DUPLICATION=1`; `FALSE_SIMILARITY=2`. These counts sum to 26. `UNRESOLVED_DUPLICATE_CAPABILITY_GROUPS=0` within the inventory above. `SHARED_IMPLEMENTATIONS_CREATED=3`; `DUPLICATE_IMPLEMENTATIONS_REMOVED=17`; `CONSUMERS_MIGRATED=17` direct source files. `NEW_MAVEN_MODULES=0`.

Before vector for P01 (the three old functions): `{"z":"é","a":[1,null,{"b":true}]}` → `c08c8ee961ee3a0ab1661a815c9a78f3622008fcef94a7343d73c684243c0dcc`; invalid object → native JSON `TypeError`. The new owner has the same positive and negative vectors. Java J01 vectors cover original value, fallback, Unicode, blank/null, and multi-value priority. F01 keeps its old exported name and default options; F02 moves the exact function body to one file.

No SQL, Flyway, Maven POM, dependency version, provider signer, canonical digest chain, credential redaction, or stage-semantic asset was edited. Test command outcomes and any unresolved regression gate must be reported with the final candidate rather than inferred from this source-level matrix.

## Current regression evidence and acceptance limit

- Java: `mvn -B test` passed the full reactor against disposable PostgreSQL 16 after `BackendCiLegacyAccountFixture` ran Flyway migrate/validate. `nq-app`: 653 tests, 0 failures, 0 errors, 146 existing skips. `ModuleBoundaryArchTest` and `PackageBoundaryArchTest` passed. A prior run without `SPRING_DATASOURCE_URL` failed only the DB-backed `nq-app` cases and is superseded by this environment-correct run.
- Frontend: `npm run build` passed TypeScript/Vite and 40/40 unit tests, including new direct compatibility vectors. The first targeted E2E run exposed three baseline English assertions against the current zh-CN default (`git show HEAD` confirmed both facts). The tests were corrected to assert the rendered Chinese labels and the safe, traceable error response rather than raw server error text. The rerun passed dashboard/Shadow Run 3/3. CI's loopback critical E2E allowlist passed 25/25 with no skips; its real-backend allowlist passed 2/2 against an isolated local backend/PostgreSQL with no skips. Product i18n behavior was not changed.
- Python: 26/26 pytest, mypy across 19 source files, and Ruff passed.
- Global: stage-semantic guard (`errors=0`), stage-assets guard (`errors=0`) and `git diff --check` passed. Modified source excludes SQL, Flyway and POM files. The disposable PostgreSQL containers and local backend were stopped after the runs. Local `REGRESSION_GREEN=true`; no remote exact-head CI outcome is asserted here.
