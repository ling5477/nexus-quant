# GateAUDIT-0C R3 Doc-Link Linux Remediation Review Acceptance Evidence

<!-- nq-runtime-scan:historical-reference:start -->

```text
Evidence classification:
HISTORICAL_EXECUTION_EVIDENCE / NON_RUNTIME_AUTHORITY

Task:
NQ-GATEAUDIT-0C-R3-DOC-LINK-LINUX-INDEPENDENT-REVIEW

Decision:
PASS / REVIEW_ACCEPTED / READY_TO_COMMIT

Provenance:
Retrospective durable transcription of the already completed
independent remediation review result supplied by the user.

This file does not perform or repeat the review.
```

## 1. Provenance integrity

```text
Source event:
NQ-GATEAUDIT-0C-R3-DOC-LINK-LINUX-INDEPENDENT-REVIEW

Source decision:
PASS / REVIEW_ACCEPTED / READY_TO_COMMIT

Independent review repeated by this task=NO
Acceptance generated by this task=NO
Candidate modified by review=NO
Independence violation=0
```

本文件仅转录用户提供的、此前已经完成的独立审查结果。本 materialization task 不是新的 reviewer，不重新执行测试，不生成新的 acceptance，也不把 materialization validation 改写为 source-review validation。

## 2. Failed exact-head baseline

```text
commit/headSha:
99c976306fb4c645251847c35ecf8c09f194b05d

CI:
33164682651

result:
completed / failure

jobs:
10/11 success

only failed job:
Diff check

failed step:
Run blocking governance regression

failed checker:
scripts/docs/check-doc-links.ps1
```

Run `33164682651` 是 immutable failed baseline，不是 GREEN CI。新的 doc-link remediation commit 与 exact-head CI 尚未产生。

## 3. Previous P1 Linux proof

```text
P1-01:
CLOSED_BY_LINUX_CI

FIXTURE_MUTATION_SELF_CHECKS=PASS
safety negatives=9/9 rejected
```

```text
P1-02:
CLOSED_BY_LINUX_CI

Java engineering standard Shadow=SUCCESS
verifier=PASS
Shadow executed=YES
artifact upload=PASS
```

```text
previous P1 code modified by doc-link remediation=NO
CI workflow modified=NO
```

## 4. Root cause

```text
classification:
POWERSHELL_PROVIDER_HIDDEN_ITEM_PORTABILITY

.agents present=YES
Test-Path .agents=PASS

baseline Get-Item without -Force
on Linux/pwsh=FAIL

checkout failure=NO
missing tracked files=NO
sparse checkout issue=NO
review-isolation residue=NO
```

Exact-head Linux CI 已完成正常 checkout，并在 `Test-Path` 通过后到达无 `-Force` 的 `Get-Item`。因此失败属于 PowerShell provider 对 hidden dot-directory 的可见性差异，而不是 repository checkout 或文件缺失。

## 5. Remediation acceptance

```text
Get-Item -Force=YES
Get-ChildItem -Force=YES

ROOT_SET_CHANGED=NO
.agents remains blocking root=YES

MISSING_ROOT_FAIL_CLOSED=PRESERVED
HISTORICAL_WARNING_POLICY=PRESERVED
CI_CHECK_WEAKENED=NO
```

变更只为显式 root 解析与递归 Markdown 枚举增加 `-Force`；没有 Linux 特判、异常吞噬、warning 降级、`continue-on-error` 或 CI bypass。

## 6. Independent source-review tests

下表均为 source independent review 的历史结果，本 materialization task 未重新执行：

| Check | PS5.1 | PS7 | Source-review result |
| --- | --- | --- | --- |
| Hidden-root positive | `PASS` | `PASS` | checked=`1`，errors=`0` |
| Missing-root negative | `ROOT_NOT_FOUND / exit 1` | `ROOT_NOT_FOUND / exit 1` | fail-closed preserved |
| Active valid link | `PASS` | `PASS` | accepted |
| Active broken link | `REJECT / ERROR` | `REJECT / ERROR` | blocking error preserved |
| Historical broken link | `WARNING / errors=0` | `WARNING / errors=0` | historical warning semantics preserved |
| Canonical candidate links | `PASS` | `PASS` | checked=`191`，warnings=`123`，errors=`0` |

## 7. Existing regression evidence

```text
Agent fixtures=12/12
malicious=6/6 rejected
previous capability=3/3 rejected
F1=4/4 rejected

authority safety=9/9 rejected
FIXTURE_MUTATION_SELF_CHECKS=PASS

lifecycle=20/20
archive=6/6
release CI isolation=PASS

TASK_ID_SPECIFIC_RUNTIME_RULES=0
GATE_SPECIFIC_ACTIVE_RUNTIME_RULES=0
ACTIVE_AUDIT_CHARTERS=1

Java verifier:
GOVERNANCE_CHECKER_RESULT=PASS
```

以上结果归属于已完成的 source independent review，不由本 materialization task 重新生成。

## 8. Findings

```text
P0=0
P1=0

P2:
SUPPLY_CHAIN_ACTION_PINNING
DEFERRED_TO_FULL_CI_SECURITY_AUDIT

GitHub Actions Node/action runtime deprecation warnings:
NON_BLOCKING / FUTURE_CI_SECURITY_MAINTENANCE

P3=0
```

Node/action runtime deprecation warnings 不升级为本轮 P1，也不在本 evidence materialization 中整改。

## 9. Non-runtime and authority boundary

本文件仅为 `HISTORICAL_EXECUTION_EVIDENCE / NON_RUNTIME_AUTHORITY`。它不是 active routing rule、machine lifecycle contract、`next_action` matcher、Skill instruction 或 Audit Charter，不覆盖 `docs/current/STATUS.md`。

Current authority 保持：

```text
work_batch=GateAUDIT-0C-R3-DOC-LINK-LINUX-REMEDIATION
work_batch_status=IMPLEMENTED|PENDING_REVIEW
work_batch_commit=NONE
work_batch_ci_run=NOT_RUN
next_action=NQ-GATEAUDIT-0C-R3-DOC-LINK-LINUX-INDEPENDENT-REVIEW
```

下一具体动作由单独的 canonical restore 与 authority reconciliation task 执行：

```text
RESTORE_CANONICAL_LAYOUT_AND_RECONCILE_DOC_LINK_REMEDIATION_ACCEPTANCE
```

<!-- nq-runtime-scan:historical-reference:end -->
